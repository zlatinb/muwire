subprojects {
    apply plugin: 'groovy'

    dependencies {
        compile 'org.codehaus.groovy:groovy-all:2.4.15'
    }

    compileGroovy {
        groovyOptions.optimizationOptions.indy = true
    }

    repositories {
        mavenCentral()
    }
}
